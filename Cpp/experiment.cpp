#include <bits/stdc++.h>
#include <limits.h>
using namespace std;
int main()
{
    long long lon =LLONG_MAX;
    int b = LONG_MAX;
    long signed int long a = 34; // NOTE any order can be taken in cpp for data type
    cout << a << " " << b;
    int *pt = nullptr;
    cout << pt;
    cin >> b;
    return 0;
}