#include <bits/stdc++.h>
#define FOR(i, n) for (int i = 0; i < (n); i++)
#define ten(n) ((int)1e##n)
using namespace std;
int main(int argc, char const *argv[])
{
    int count = 0;
    FOR(i, ten(4))
    {
        count++;
        cout << count << endl;
    }
    return 0;
}
