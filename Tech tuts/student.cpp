#include <bits/stdc++.h>
using namespace std;

//1. break statement is only used in loops only
//2. -ve value are not allowed in 
int main()
{
    if (0)
        cout << "db";
    else if (true)
        cout << "r";
    else
        cout << "cr";
    return 0;
}